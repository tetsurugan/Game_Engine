import type { StoryClassHandler } from "../storyClassTypes";

/**
 * Placeholder for `distorted` (unreliable narration / UI). Future: use
 * `beforeChoiceVisible` for text mutation and `beforeEndingResolve` for
 * evaluationState vs true state.
 */
export const distortedHandler: StoryClassHandler = {};
