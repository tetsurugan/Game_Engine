import type { StoryClassHandler } from "../storyClassTypes";

/**
 * Placeholder for **`possessed`** stories: narratives where **non-rational or
 * impulse-dominant systems** partially or heavily override deliberate player
 * intent — e.g. berserker rage, animal survival or territorial POV, compulsions,
 * frenzy, panic override, curse-driven behavior, feral states, or classic
 * “possession.” Not limited to the supernatural.
 *
 * Future work: narrow or mutate choices in `beforeChoiceVisible` / `beforeChoiceResolve`,
 * force `nextSceneId`, append instinctive consequences, or read `hiddenTruth` vs
 * visible state for what the creature “knows.”
 */
export const possessedHandler: StoryClassHandler = {};
