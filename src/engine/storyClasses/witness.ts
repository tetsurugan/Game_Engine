import type { StoryClassHandler } from "../storyClassTypes";

/**
 * Placeholder for `witness` (indirect agency). Future: reshape visibility
 * and ending evaluation to reflect interpretation rather than direct action.
 */
export const witnessHandler: StoryClassHandler = {};
